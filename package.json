{
    "name": "2026-rojam-entry",
    "private": true,
    "scripts": {
        "wally": "wally install && rojo sourcemap default.project.json --output sourcemap.json && wally-package-types --sourcemap sourcemap.json Packages"
    }
}